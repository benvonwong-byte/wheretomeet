{
  "name": "wheretomeet",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "fetch:venues": "node scripts/fetch-venues.mjs",
    "fetch:subway": "node scripts/fetch-subway.mjs",
    "enrich:wikidata": "node scripts/enrich-wikidata.mjs",
    "enrich:google": "node scripts/enrich-google.mjs"
  },
  "dependencies": {
    "leaflet": "^1.9.4"
  },
  "devDependencies": {
    "@types/leaflet": "^1.9.12",
    "typescript": "^5.6.0",
    "vite": "^6.0.0",
    "vitest": "^3.0.0"
  }
}
