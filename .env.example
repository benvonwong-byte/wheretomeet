# Google Places (New) key for venue enrichment (stars/price/photos/closed).
# Store the real key in 1Password, then: op-resolve  →  .env.local
GOOGLE_PLACES_API_KEY=op://Personal/Google Places API/credential
